请问下列说法正确的是：

- A. 在使用调试器时，软件中的反调试检测无法绕过。
- B. 对程序进行动态调试时，可以在运行过程中修改寄存器或内存的值。
- C. 在没有符号表的情况下，反编译工具仍然可以识别出部分函数边界。
- D. 使用 `ltrace` 可以跟踪程序调用的库函数（如 `printf`、`strcmp` 等）。
- E. 在ELF文件中，`.data` 段存放了所有变量数据。

FLAG形式为正确选项按字母序升序排列得到的大写字母列表，例如：ADE，注意：本题只有一次答题机会！请谨慎作答！

Challenge Type: StaticAttachment 

this challenge has no attachment