某程序关键验证逻辑如下：
```c
for (i = 0; i < 4; i++) {
    if ((input[i] + i) != target[i])
        return 0;
}
return 1;
```
已知：`target = {0x42, 0x44, 0x46, 0x48}`. 

请问正确的 `input` 应为多少？

- A. ACEG
- B. ABCD
- C. BCDE
- D. BDFH

FLAG形式为正确选项的单一大写字母，例如：A，只有一次提交机会，请谨慎作答！

Challenge Type: StaticAttachment 

this challenge has no attachment