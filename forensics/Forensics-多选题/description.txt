在电子取证中，以下哪些描述是正确的？

A. 可以通过篡改PNG文件IHDR块中的宽度或高度字段隐藏图像的部分内容。

B. NTFS文件系统的$MFT（主文件表）中记录了文件的创建、修改、访问时间戳，这些时间戳无法被用户态工具篡改，因此在取证中可作为可信的时间线依据。

C. 在Wireshark中使用显示过滤器 tcp.stream eq N 可以隔离特定TCP会话，并通过”Follow TCP Stream“功能重组该会话的完整应用层数据。

D. 在Wireshark中对TLS加密流量进行解密时，若已导入服务器的RSA私钥，即可解密使用RSA密钥交换的会话；同理，对于使用ECDHE密钥交换的会话，导入服务器私钥后同样可通过派生临时会话密钥来解密流量。

E. 在进行内存转储取证时，即使磁盘上的恶意文件已被攻击者使用安全删除工具彻底擦除，若该文件曾被加载执行，仍可能从内存转储中恢复出该可执行文件的内容。

FLAG形式为正确选项按字母序升序排列得到的大写字母列表，例如：ADE，注意：本题只有一次答题机会！请谨慎作答！

Challenge Type: StaticAttachment 

this challenge has no attachment