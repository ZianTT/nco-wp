关于“注入攻击”的核心原理与防御逻辑，以下哪些描述是正确的？

A.  跨站脚本执行（XSS） 在本质上是一种在浏览器生效的注入攻击。

B. 只有数据库注入攻击（SQLi）和命令注入攻击 (CMDi) 属于注入攻击。

C.  只要建立一套完善的“黑名单”过滤机制（如过滤单引号、分号、双重编码），就能从根本上消除所有注入风险。

D.  注入漏洞仅存在于直接接收用户 HTTP 请求的 Web 接口中，数据库内部存储的数据是安全的。

E.  SSRF（服务端请求伪造）在本质上也可以看作是一种注入攻击，其注入的对象是服务端发起的网络请求目标（URL/IP）。

FLAG形式为正确选项按字母序升序排列得到的大写字母列表，例如：ADE，注意：本题只有一次答题机会！请谨慎作答！


Challenge Type: StaticAttachment 

this challenge has no attachment